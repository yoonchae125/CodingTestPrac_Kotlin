package kakao_2019_blind

class CandidateKey {
    var answer = 0
    fun solution(relation: Array<Array<String>>): Int {
        val numOfCols = relation[0].size

        for(i in 0 until numOfCols){

        }

        return answer
    }
}